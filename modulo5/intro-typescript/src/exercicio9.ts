let idade:number;
let escolaridade: boolean;
let horasLivres: number;
let modalidade: string;

function validaLabenu(numbe:number, escolaridade:boolean, horasLivres:number, modalidade:string):boolean{


return
}